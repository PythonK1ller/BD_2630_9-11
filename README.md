# Proyecto 1 — Bases de Datos (PUJ)

Sistema de cafeterías para los servicios de alimentación de la Pontificia Universidad Javeriana.
Curso de Bases de Datos — Ing. Julio Omar Palacio Niño, M.Sc.

## Integrantes

| Nombre completo | Usuario GitHub |
|---|---|
| Pablo Alfonso Jimenez Becerra | @PythonK1ller  |
| Cristian Almeida Velasco | @KHryyz |
| Samuel Alejandro Baron Alba | @SamuelBaron08 |
| Samuel Barrera Leon | ba.samuel@javeriana.edu.co |


**DBMS objetivo: Oracle.** El enunciado usa `VARCHAR2` y
`number(10,0) generated by default as identity`, así que todo el SQL está escrito
en dialecto Oracle. No es portable a PostgreSQL/MySQL sin traducción.

---

## Estructura del repositorio

```
.
├── README.md
├── .gitignore
├── sql/
│   ├── proyecto1_completo.sql      ← script maestro: ejecuta todo en orden
│   ├── 99_verificacion.sql         ← comprueba requisitos y filas de cada vista
│   ├── 01_ddl/
│   │   ├── 00_borrar_tablas.sql    ← limpieza para re-ejecutar desde cero
│   │   └── 01_crear_tablas.sql     ← DDL: 5 tablas, PK, FK, UNIQUE, CHECK
│   ├── 02_dml/
│   │   ├── 00_poblar_plsql.sql ← poblamiento completo en un bloque PL/SQL
│   │   ├── 01_edificios.sql    ← (alternativa) mismos datos como INSERT estáticos
│   │   ├── 02_pisos.sql
│   │   ├── 03_cafeterias.sql
│   │   ├── 04_colaboradores.sql
│   │   ├── 05_metas.sql
│   │   └── 06_resincronizar_identities.sql
│   ├── 03_vistas/
│   │   └── vista_1.sql … vista_7.sql
│   └── 04_permisos/
│       └── 01_grants.sql           ← GRANT SELECT … TO JPALACIO
├── entrega/
│   └── proyecto1_entrega_unico.sql ← todo concatenado, para subir a Brightspace
├── scripts/
│   └── generar_datos.py            ← opcional: regenera los INSERT estáticos
└── docs/
    ├── informe.md                  ← plantilla del informe de Brightspace
    └── evidencias/                 ← pantallazos de los resultados
```

## Cómo ejecutar

Desde SQL Developer, parado en la carpeta `sql/`:

```sql
SQL> @proyecto1_completo.sql
```

Para la entrega en Brightspace use `entrega/proyecto1_entrega_unico.sql`, que
contiene todo concatenado en un solo archivo (el script maestro usa `@@` y solo
funciona con la estructura de carpetas al lado).

Si ya hay objetos de una corrida anterior, descomente primero la línea que llama
a `00_borrar_tablas.sql` dentro del script maestro.

### Dos formas de poblar (use una, nunca las dos)

1. **`sql/02_dml/00_poblar_plsql.sql`** — un solo bloque PL/SQL anónimo que
   genera las 5 tablas con bucles y `DBMS_RANDOM`. Es la opción por defecto del
   script maestro: no depende de Python y cabe en un archivo corto. Para variar
   los datos, cambie `c_semilla` dentro del bloque.
2. **`sql/02_dml/01_edificios.sql` … `05_metas.sql`** — los mismos datos como
   `INSERT` literales, uno por fila. Útil si prefiere que el script de entrega
   muestre cada tupla de forma explícita. Se regeneran con
   `python3 scripts/generar_datos.py` (cambie `SEMILLA` para otros valores).

Ambas cumplen todos los lineamientos del enunciado; solo cambian las cifras
exactas (986 metas la estática, 1095 la de PL/SQL).

## Qué garantiza el poblamiento

| Requisito del enunciado | Estado |
|---|---|
| ~40 edificios | 40 |
| ~5 pisos por edificio | 200 pisos (4–6 por edificio) |
| ~12 cafeterías en el campus | 12, repartidas en 10 edificios |
| 20–30 colaboradores | 26 (PLANTA y TEMPORAL) |
| Tabla META ≥ 500 registros | 1095 (PL/SQL) · 986 (estático) |
| Periodo continuo ≥ 6 meses | 2025-09-01 → 2026-02-28 |
| ≥ 2 edificios sin cafetería | 30 |
| ≥ 3 pisos sin cafetería | 188 |
| ≥ 5 colaboradores con metas en **todas** las cafeterías | 5 (ids 1–5) |
| ≥ 2 colaboradores sin ninguna meta | 2 (ids 25 y 26) |
| Meses con metas incumplidas | noviembre 2025 y febrero 2026 |

El periodo cruza a propósito el cambio de año, para que la consulta 3 (agrupada
por año) devuelva más de un grupo y no una sola fila.

## Filas que devuelve cada vista con estos datos

| Vista | Filas |
|---|---|
| VISTA_1 | 697 |
| VISTA_2 | 200 |
| VISTA_3 | 5 (4 grupos + gran total) |
| VISTA_4 | 24 |
| VISTA_5 | 8 |
| VISTA_6 | 5 |
| VISTA_7 | 11 (10 edificios + totales) |

Ninguna queda vacía, como pide la nota final del enunciado. (Cifras del
poblamiento PL/SQL; con los `INSERT` estáticos varían un poco pero ninguna
vista queda en cero.)

**Detalle del recorrido de fechas en el bloque PL/SQL:** para cada par
(colaborador, cafetería) se avanza en saltos de 24 o 13 días desde un desfase
propio. El paso es **impar a propósito**: con un múltiplo de 7 todas las fechas
del par caerían siempre en el mismo día de la semana, y los pares que
arrancaran en domingo (día no operativo) se quedarían sin ninguna meta. Como
las fechas son estrictamente crecientes dentro de cada par, la llave natural
`(idcolaborador, idcafeteria, fechameta)` nunca se repite y no hace falta
manejar `DUP_VAL_ON_INDEX`.

## Decisiones de diseño que conviene defender en el informe

**1. `fechameta` es `DATE`, no `INTEGER`.** El diagrama del enunciado la dibuja
como `INTEGER`, pero el texto la describe como "fecha diaria de la meta" y las
consultas 3 y 4 exigen agrupar por año y por mes. Un entero no permite
`EXTRACT(YEAR …)` sin trucos.

**2. Nombres `valormeta` / `valorreal`.** El diagrama usa esa forma y el texto
escribe `valor_meta` / `valor_real`. Se siguió el diagrama. Si el profesor exige
la otra, es un `ALTER TABLE meta RENAME COLUMN` y ajustar las 7 vistas.

**3. La consulta 6 se interpreta sobre los edificios que tienen cafetería.** El
enunciado pide un colaborador con metas en cafeterías de *todos* los edificios,
pero al mismo tiempo obliga a que existan edificios sin ninguna cafetería. Con la
lectura literal la vista sería vacía siempre, lo que contradice la nota "asegúrese
que todas las consultas tengan tuplas de resultado". El universo de comparación
se calcula dinámicamente con una subconsulta, no con una constante quemada.

**4. La comisión se calcula sobre las ventas reales**, `valorreal * comision/100`,
tal como dice la definición del atributo ("porcentaje que se paga sobre las
ventas reales").

**5. `GROUPING SETS` en vez de `ROLLUP`** en las vistas 3 y 7: `ROLLUP` añadiría
subtotales intermedios que el enunciado no pide. `GROUPING()` en el `ORDER BY`
fuerza que la fila del total quede de última.

**6. La fila "TOTALES" de la vista 7 no es la suma vertical de las columnas.** Se
cuenta `COUNT(DISTINCT col.id)`, así que un colaborador que trabaja en dos
edificios aparece en dos filas pero se cuenta una sola vez en el total. Es lo
correcto conceptualmente (son personas distintas, no una sumatoria), pero rompe
la aritmética del ejemplo del enunciado, así que vale la pena mencionarlo.

**7. Los `id` se insertan de forma explícita.** Como las columnas son
`GENERATED BY DEFAULT` (no `ALWAYS`), Oracle lo permite y las llaves foráneas
quedan deterministas. El efecto secundario es que la secuencia interna no avanza,
por eso `06_resincronizar_identities.sql` la reposiciona con
`START WITH LIMIT VALUE`.

## Pendientes antes de entregar

- [ ] Cada integrante debe montar el proyecto **en su propio usuario** (−2.0 a
      quien no lo haga).
- [ ] Tomar los pantallazos de cada vista y guardarlos en `docs/evidencias/`.
- [ ] Completar `docs/informe.md` con nombres y cuentas de usuario de todos los
      integrantes.
- [ ] Subir a Brightspace el informe **y** el script `.sql` (−1.0 si falta el script).
